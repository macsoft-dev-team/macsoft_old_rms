import { Modal,Button } from "react-bootstrap";
import { useDispatch, useSelector } from "react-redux";
import { setConfigModal } from "../../lib/reducer/deviceSlice";
export default function ConfigurationRequest() {


    return (
           
           <section>

           </section>
    );
}